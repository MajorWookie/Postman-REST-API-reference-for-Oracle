# Local conversion pipeline (Node project, specs, node_modules) — not published
pipeline/
# Machine-readable twin of REPORT.md, rewritten by every build
report.json

# Claude Code
CLAUDE.md
.claude/

# Downloaded Oracle specs, if fetched at the repo root
*swagger.json
*openapi.json

# OS / editor
.DS_Store
